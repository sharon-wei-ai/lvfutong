{
  "name": "lvfutong",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "refresh": "node scripts/refresh.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
